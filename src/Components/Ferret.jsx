import React from 'react';
import {useParams, useHistory} from 'react-router-dom';

const Ferret = () => {

  return (
  <div>
    <h1>If we knew we couldn't find a Ferret emoji then we wouldn't have name our show page Ferrets, so here's otter instead.</h1>
    <h1>
      🦦
    </h1>
    <h1>You're welcome!</h1>
    </div>
  );
};

export default Ferret;
